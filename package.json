{
  "name": "pi-zen-fallback",
  "version": "1.0.0",
  "description": "Auto-fallback between free OpenCode Zen models for the pi coding agent, with self-registration on a clean install.",
  "keywords": ["pi-package", "pi-extension", "opencode", "zen", "fallback"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions"]
  }
}
